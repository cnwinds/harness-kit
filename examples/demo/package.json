{
  "name": "@harnesskit/example-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Full-stack HarnessKit demo — server + React client",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --workspace @harnesskit/example-minimal-server run dev",
    "dev:client": "npm --workspace @harnesskit/example-minimal-react run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
